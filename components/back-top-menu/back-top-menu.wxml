<!--components/back-top-menu/back-top-menu.wxml-->
<view class="back-top-menu">
  <image class="menu-img" src="image/back.png" data-url="{{backUrl}}" bindtap="back" wx:if="{{backable}}"></image>
  <view class="title">{{title}}</view>
</view>